// Objective:
// Build a React component that fetches user data from an API and displays it in a structured, user-friendly format. The goal is to test API integration, state management, and conditional rendering.

// Requirements

// API Endpoint:

// Use a public API or a mock API such as:

// https://jsonplaceholder.typicode.com/users

// Each user object contains:

// id, name, username, email, address, phone, website, company.

// Fetch Data:

// Fetch user data when the component mounts.

// Use fetch or axios.

// Handle asynchronous operations properly.

// Display Data:

// Show a list of users with the following fields:

// Name

// Email

// Phone

// Website

// Optional: display address (street, city) and company name.

// Loading State:

// Show a loading indicator while fetching data.

// Error Handling:

// Display an error message if the API call fails.

// Search / Filter (Optional Bonus):

// Add a search input to filter users by name or email in real-time.